#include<bits/stdc++.h>
using namespace std;

void solve() {
    cout << "I am a senior student, and I know better than you, so I do not need help. No thank you for wasting so much of my time." << endl;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

#ifndef ONLINE_JUDGE
    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);
#endif

    int t_case = 1;
    // cin >> t_case;

    while(t_case--) {
        solve();
    }

    return 0;
}